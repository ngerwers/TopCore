<Shell
    x:Class="TopCore2.AppShell"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:local="clr-namespace:TopCore2"
    Title="TopCore2">

    <TabBar>
        <ShellContent
            Title="Home"
            ContentTemplate="{DataTemplate local:MainPage}"
            Route="MainPage" />

        <ShellContent
            Title="Listen"
            ContentTemplate="{DataTemplate local:ListenPage}"
            Route="ListenPage" />

        <ShellContent
            Title="Über Uns"
            ContentTemplate="{DataTemplate local:UeberUnsPage}"
            Route="UeberUnsPage" />
    </TabBar>

</Shell>